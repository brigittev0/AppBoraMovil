<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.AppBoraBora" parent="Theme.Material3.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/purple_1200</item>
        <item name="colorControlHighlight">@color/color_hint</item>
    </style>

    <style name="Theme.AppBoraBora" parent="Base.Theme.AppBoraBora">
        <!-- Transparent system bars for edge-to-edge. -->
        <!-- Barra del sistema -->
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:statusBarColor">@color/purple_1200</item>
        <item name="android:windowLightStatusBar">false</item>
    </style>

    <style name="Theme.AppBoraBora.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="MyRatingBar" parent="@android:style/Widget.RatingBar">
        <item name="android:minHeight">15dp</item>
        <item name="android:maxHeight">15dp</item>
        <item name="colorControlNormal">@color/white</item>
        <item name="colorControlActivated">@color/purple_800</item>
    </style>

    <style name="Theme.AppBoraBora.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.AppBoraBora.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
</resources>