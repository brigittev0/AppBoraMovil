<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Fondo cuando el elemento está seleccionado -->
    <item android:state_pressed="true">
        <layer-list>
            <!-- Fondo personalizado -->
            <item>
                <shape>
                    <solid android:color="#FFFFFF" />
                    <stroke android:width="1dp" android:color="@color/gray_line_form" />
                    <corners android:radius="50dp" />
                </shape>
            </item>
            <!-- Flecha de despliegue -->
            <item
                android:drawable="@drawable/baseline_keyboard_arrow_down_24"
                android:gravity="start|center_vertical"
                android:left="275dp" />
        </layer-list>
    </item>
    <!-- Fondo predeterminado -->
    <item>
        <layer-list>
            <!-- Fondo personalizado -->
            <item>
                <shape>
                    <solid android:color="#FFFFFF" />
                    <stroke android:width="1dp" android:color="@color/gray_line_form" />
                    <corners android:radius="50dp" />
                </shape>
            </item>
            <!-- Flecha de despliegue -->
            <item
                android:drawable="@drawable/baseline_keyboard_arrow_down_24"
                android:gravity="start|center_vertical"
                android:left="275dp" />
        </layer-list>
    </item>
</selector>